<?xml version="1.0" encoding="utf-8"?>
<!-- Branded launch splash: teal background + centered app icon.
     Automatically replaced once the Flutter engine draws its first frame. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/clinitrack_splash" />

    <item>
        <bitmap
            android:gravity="center"
            android:src="@mipmap/ic_launcher" />
    </item>
</layer-list>
